import { Container } from '../Container/Container';
import { Todo } from '../Todo/Todo';
import s from './Main.module.scss';

export const Main = () => {
  return (
    <main>
      <Todo />
    </main>
  );
};
